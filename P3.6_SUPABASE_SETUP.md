# P3.6 · Supabase 双人魔法邮局上线清单

P3.6 沿用当前 Next.js 部署方式，不强制改成纯静态导出。网站原有书影数据继续保存在浏览器 `localStorage`；只有双人邮局的账号、邀请码和信件使用 Supabase。

## 1. 创建 Supabase 项目

1. 在 Supabase 新建项目，保存项目 URL 与 Publishable key。
2. Authentication → Sign In / Providers → Email：启用邮箱登录。
3. 邮件模板使用 OTP 验证码，在模板正文中加入 `{{ .Token }}`。
4. Authentication → URL Configuration：把 Site URL 和 Redirect URLs 设置为网站正式地址（GitHub Pages 使用完整 Pages 地址）。

## 2. 建表与权限

安装并登录 Supabase CLI 后，在项目目录执行：

```bash
supabase link --project-ref 你的项目ID
supabase db push
```

迁移文件会创建双人邮箱、成员、邀请码、留言、回复、反应、未读状态，并开启 RLS。普通浏览器客户端拿不到 Service Role Key；未成为这座邮局的成员时也无法读取任何信件。

## 3. 配置 Edge Function 密钥

设置馆主邮箱和一次性的建站口令：

```bash
supabase secrets set MAILBOX_OWNER_EMAIL=你的邮箱 MAILBOX_BOOTSTRAP_SECRET=一段足够长的随机口令
```

`SUPABASE_URL`、`SUPABASE_ANON_KEY`、`SUPABASE_SERVICE_ROLE_KEY` 由 Supabase 托管函数环境自动提供，不要把 Service Role Key 写入前端或提交到 GitHub。

部署两个函数：

```bash
supabase functions deploy mailbox-admin
supabase functions deploy redeem-mailbox-invite
```

## 4. 配置网站环境变量

复制 `.env.example` 为本机的 `.env.local`，填写：

```env
NEXT_PUBLIC_SUPABASE_URL=https://你的项目ID.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=你的PublishableKey
```

GitHub Pages 自动构建时，也要在仓库 Settings → Secrets and variables → Actions 中保存这两个值，并在现有构建工作流的 `npm run build` 步骤注入同名环境变量。

没有配置环境变量时，网站不会报错或泄漏信息，只会在邮局页显示“邮路尚未接通”。

## 5. 初始化馆主与邀请朋友

1. 用馆主邮箱进入“猫头鹰邮局”，输入邮件收到的验证码。
2. 点击“馆主首次建站”，输入第 3 步设置的建站口令；该操作只能由指定馆主邮箱成功执行。
3. 打开站长工具（`Ctrl + Shift + E`）→“猫头鹰邮局”。
4. 输入朋友邮箱并生成一次性邀请码，把网站地址和邀请码单独发给朋友。
5. 朋友使用自己的邮箱验证码登录，再填写邀请码、昵称和头像符号。

邮局最多两名成员。同一个邀请码只能使用一次，默认 48 小时失效；错误尝试 30 分钟内最多 5 次。

## 6. 验收建议

- 分别用两个浏览器或无痕窗口登录馆主和朋友账号。
- 确认第三个账号、失效邀请码和错误邮箱都无法进入。
- 朋友投递首封信，确认它只对朋友本人和馆主可见，馆主批准后双方实时出现。
- 测试回复、置顶、删除、反应、未读角标、在线状态和“正在写信”。
- 在站长工具停用朋友账号，确认朋友立即失去数据库访问权。
- 确认 GitHub 仓库中没有 `.env.local`、Service Role Key 或建站口令。
