# P3.6 · 猫头鹰邮局云端升级说明

猫头鹰邮局已经从浏览器本地留言簿升级为 Supabase 私人云端邮局。

## 当前数据来源

- 书籍、影视、CP 与分段笔记以 `archive_entries` 为云端主数据源。
- 信件、回信、反应和未读状态分别保存在 `letters`、`letter_reactions` 与 `mailbox_read_states`。
- 登录身份来自 Supabase Auth；浏览器不再允许模拟切换成另一位成员。
- 浏览器仍保存档案缓存和未投递草稿，用于断网恢复，不再作为跨设备主数据。

## 使用方式

1. 开启邀请函后，使用邮箱 Magic Link 登录私人云端。
2. 登录后自动从 Supabase 读取书、影、CP 和笔记。
3. 新增、编辑、删除或修改笔记时自动写入云端。
4. 猫头鹰邮局的信件与反应通过 Supabase Realtime 更新。
5. 站长工具中的“云端同步”可查看状态、重新读取或退出账户。

## 旧版本地留言

旧版 `randi-local-mailbox-v1` 数据不会被自动删除。由于旧留言使用可切换的本地假身份，无法安全映射为两个真实 Supabase 账户，因此暂不自动上传；完成第二位成员邀请与身份绑定后再进行一次显式迁移。
