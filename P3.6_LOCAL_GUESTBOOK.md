# P3.6 · 纯前端哥特魔法留言墙

猫头鹰邮局已经改为完全在浏览器中运行的本地留言墙，不需要账号、邀请码、环境变量、Supabase 或任何服务端接口。

## 数据保存位置

- 书籍、影视和 CP 档案继续使用原有的浏览器 `localStorage`。
- 留言墙使用独立的 `randi-local-mailbox-v1` 存储项。
- 草稿使用独立的 `randi-mailbox-draft-v1` 存储项。
- 清空留言墙不会影响书影和 CP 档案。

## 使用方式

1. 打开“魔法邮局”。
2. 在两枚头像中选择本次留言使用的署名。
3. 点击“留下魔法信笺”，可填写正文、信件类型、心情印章并关联本地书影档案。
4. 留言支持回信、30 分钟内修改、收回、置顶和魔法反应。
5. 打开站长工具（`Ctrl + Shift + E`）→“猫头鹰邮局”，可修改留言墙名称、两位署名和头像。

## 纯前端限制

留言只保存在当前浏览器。换设备、使用无痕窗口或清除网站数据后，原留言不会出现；不同访客设备之间也不会自动同步。这是纯前端模式的预期行为。
